generator client {
  provider      = "prisma-client-js"
  output        = "../../lib/generated/prisma"
  binaryTargets = ["native", "rhel-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
}
